import { Nunito, Lora } from "next/font/google";

export const nunito = Nunito({
  weight: ["700", "900"],
  subsets: ["latin"],
});

export const lora = Lora({
  weight: "400",
  subsets: ["latin"],
});